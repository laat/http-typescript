{
  "name": "http-bin-contracts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "files": [
    "**/*.d.ts",
    "**/*.schema.json"
  ],
  "main": "index.js",
  "scripts": {
    "compile": "tsc --project tsconfig.release.json && yarn gen-schema",
    "gen-schema": "http-ts gen-schema",
    "new": "http-ts new",
    "test": "jest",
    "test-production": "bash test-production.sh",
    "validate": "http-ts validate"
  },
  "devDependencies": {
    "@types/expect": "^1.20.3",
    "@types/jest": "^24.0.0",
    "expect": "^24.1.0",
    "http-typescript": "0.0.0",
    "jest": ">=22.0.0 <24.0.0",
    "ts-jest": "^23.10.5",
    "typescript": "^3.3.3"
  }
}
